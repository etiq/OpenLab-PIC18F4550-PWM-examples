#include "delay.h"
#include <plib\pwm.h>
void PwmCycle(unsigned char period, unsigned int Duty_cycle)
    {
while(Duty_cycle != 0xFF)
        {
        }                                                /*  : 10bit resolution) */
    }